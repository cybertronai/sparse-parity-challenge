name: Submit Solution
description: Submit a Python function that solves sparse parity
title: "Submission: "
labels: ["submission"]
body:
  - type: markdown
    attributes:
      value: |
        Paste your solution below. It will be automatically evaluated for accuracy, speed, and energy cost (DMC).

        Your function must use only `numpy` (imported as `np`). It runs in a sandboxed GitHub Actions environment with a 60-second timeout.

        **Type your method name in the issue title above** (after "Submission: ").

  - type: textarea
    id: code
    attributes:
      label: Solution
      description: Replace the entire contents below with your solve() function.
      value: |
        def solve(x, y, n_bits, k_sparse):
            # Your code here. numpy is available as np.
            # Return sorted list of k secret bit indices.
            pass
      render: python
    validations:
      required: true
